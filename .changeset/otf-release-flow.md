---
"tva": minor
---

Add a lightweight Changesets-based app release flow (prepare + finalize workflows, 1Password-loaded secrets, GPG-signed release commit) plus API versioning and app release docs.
